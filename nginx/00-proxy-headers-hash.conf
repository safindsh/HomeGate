# HomeGate uses several custom proxy headers across its MCP and dashboard
# locations. Increase the hash table so nginx can build it without warnings.
proxy_headers_hash_max_size 1024;
proxy_headers_hash_bucket_size 128;
