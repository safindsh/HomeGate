# Ограничение частоты для стартовой страницы.
# Нормальная работа: страница + API + пять картинок раз в 30 секунд.
# Тормозит перебор пароля, не мешая обычному использованию.
limit_req_zone $binary_remote_addr zone=landing:10m rate=90r/m;
