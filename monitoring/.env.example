# Скопировать в /opt/monitoring/.env, chmod 600
# Пароль: openssl rand -base64 18 | tr -d /+= | head -c 20
GRAFANA_PASSWORD=<сгенерировать>

# Адрес или домен, по которому открывается Grafana.
# Меняется при переезде сервера. Пример: 192.168.1.50 или home.example.com
SERVER_HOST=<ip-или-домен>
